name = "rm-boot-backend"
main = "src/server.ts"
compatibility_date = "2026-03-28"
compatibility_flags = [ "nodejs_compat" ]

[build]
# Comando ajustado para o seu package.json
command = "npm install && npm run build"